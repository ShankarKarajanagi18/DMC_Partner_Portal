# DMC Portal

Static deployment copy of `dmc_portal.html` for GitHub and Vercel.

## Deploy

1. Push this folder to GitHub.
2. Import the repo in Vercel.
3. Deploy with the default static settings.

The root URL is rewritten to `dmc_portal.html` by `vercel.json`.
